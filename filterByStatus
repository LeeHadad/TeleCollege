#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <algorithm>
#include "mainHeader.h"

int main()
{
	Customer array[5];
	strcpy(array[0].firstname, "Maor");
	strcpy(array[0].lastname, "Yatskan");
	array[0].status = new_lead;
	strcpy(array[1].firstname, "Lee");
	strcpy(array[1].lastname, "Hadad");
	array[1].status = new_lead;
	strcpy(array[2].firstname, "Sivan");
	strcpy(array[2].lastname, "Tetro");
	array[2].status = call_later;
	strcpy(array[3].firstname, "Liron");
	strcpy(array[3].lastname, "Vaknin");
	array[3].status = success;
	strcpy(array[4].firstname, "Limor");
	strcpy(array[4].lastname, "Shavit");
	array[4].status = irrelevent;
	Customer filtered_array[5];
	int count = 0;
	for (Customer c : array) {
		if (c.status == 1) {
			filtered_array[count] = c;
			count++;
		}
	}
	for (int i = 0; i < count; i++) {

		std::cout << filtered_array[i].firstname <<" "<< filtered_array[i].lastname<< std::endl;
	}
	system("pause");
	return 0;
}
